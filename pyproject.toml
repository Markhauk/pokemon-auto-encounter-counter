[project]
name = "auto-encounter-counter-pokemon"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "PySide6",
    "opencv-python",
    "mss",
    "numpy",
]

[project.scripts]
pokemon-encounter-counter-cli = "run:main"
pokemon-encounter-counter-gui = "app.main:main"
